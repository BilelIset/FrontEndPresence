version: '3'

services:
  web:
    image: nginx:alpine
    volumes:
      - ./dist/presence/browser:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "8080:80"
    restart: always
